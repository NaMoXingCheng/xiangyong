# 更新日志

本项目所有值得记录的变更都写在这里。

---

## v0.4.0

> 对比基准：[`2a79d76`](https://github.com/NaMoXingCheng/xiangyong/commit/2a79d76)（上一版）

### 📊 与上一版的差异

| 文件 | 上一版 | 本版 | 变化 |
|---|---:|---:|---:|
| `public/app.js` | 1,209 行 | 2,704 行 | +1,495 |
| `ai.js` | 174 行 | 893 行 | +719 |
| `public/style.css` | 594 行 | 1,119 行 | +525 |
| `server.js` | 1,487 行 | 1,985 行 | +498 |
| `public/sound.js` | — | 504 行 | 🆕 新增 |
| `score.js` | — | 256 行 | 🆕 新增 |
| `electron-main.js` | 44 行 | 150 行 | +106 |
| `build.iss` | 39 行 | 59 行 | +20 |
| **合计**（应用代码与文档） | **6,294 行** | **10,542 行** | **约 1.67 倍** |

> 另有新增的 `wordlist.txt`（40,001 行中文分词词表）与 `测试脚本/`（33 个验证脚本）未计入上表。
> 上表由 `node 工具/版本对比.js 2a79d76` 生成，可复现。

---

### ✨ 新功能

上一版完全没有的能力。

**📖 年度报告**

- 页数按实际聊天量动态生成（不再固定 12 页，上限 15 页）
- 页与页之间加入动画与元素联动；页码改为鼠标移入才显示
- 最后一页由 AI 写一段只属于你们的寄语，另有相处建议与待办锐评

**🔊 界面音效与背景音乐**（新增 `public/sound.js`，504 行）

- 所有音效与 BGM 均为 Web Audio 现场合成，**不打包任何音频文件**（零体积、零版权）
- 覆盖点击 / 翻页 / 开关 / 成功 / 报错 / 报告开场与收尾等场景
- 设置面板可单独调节响度

**💯 可解释的关系指数**（新增 `score.js`，256 行）

- 把评分从 `server.js` 里剥出来成为独立纯模块（信号层 → 三指数）
- 主动 / 被爱 / 冷淡三个指数由多个信号合成，每个指数带 `parts`，点开能看到「这 71 分是怎么来的」
- 整体与分段趋势共用同一实现，折线末端与仪表盘数字口径一致

**🧠 三档本地模型可选**（上一版只有固定的 Qwen2.5-3B）

- 轻 / 标准 / 专业三档（3B / 7B / 14B），按显存档位自动推荐
- 每档都有模型信息与显存门槛说明，装完即用

**💤 显存闲置归还**

- 分析结束闲置 120 秒后，自动把模型从显存搬回内存并尽量压缩体积
- 下次要用时再搬回来 —— 打游戏时不至于和游戏抢显存

**🖼 联系人右键自定义头像**

- 右键联系人即可更换头像，支持选图或直接粘贴

**🔒 防多开**（`electron-main.js` 44 → 150 行）

- 单实例锁：重复启动会唤起已有窗口，不会开出第二个应用、也不会抢端口

**🔤 热词榜中文分词**（新增 `wordlist.txt`，40,001 行）

- 热词榜改用中文分词 + 随包词表选词，不再出现「天早起跑」这类跨词碎片

---

### ⚡ 优化与修复

已有能力的改进。

**🤖 AI 链路**

- **模型下载源全部换成国内 ModelScope**（上一版用 `hf-mirror.com` / `huggingface.co`），并配了镜像兜底与 sha256 校验
- **断点续传增强**：分片下载 + 卡死检测（20 秒无数据即重连），弱网下不再整包重来
- **Vulkan 加速**：核显 / 独显都能吃到；没有可用 GPU 时自动回退 CPU
- **相处建议、待办锐评、报告寄语改为真 AI 生成**（上一版是服务端精修模板 + 种子变体）
- AI 说出口的每个数字都会与真实统计核对，**编造的数字会被拦下**

**🔊 音效质量**

- 重新设计音色：短音效全部改用**整数倍泛音**，修掉不谐音带来的金属味
- 电平整体提高约一倍，并把提示音基频抬到笔记本人声喇叭能响的区间
- 短音效混响量下调（长音保留），点击音不再糊成一片
- 总线加限幅器，大音量下不会削波

**📊 数据与展示**

- 承诺追踪重做：从聊天记录里认出的承诺可自行增删改，并跟踪兑现情况
- 数字显示统一收口，修掉不同字体「老式数字」导致的高低不齐
- 液态玻璃质感增强，层级与景深更清楚

**⚡ 性能与稳定性**

- 修掉了「每次点击都会报一个错」与长列表滚动卡顿
- 移除启动脚本里的 `--disable-gpu`：实测帧间隔从 57ms 改善到 **6ms**
- AI 状态轮询加了内容缓存（内容没变就不重建 DOM），窗口最小化时暂停轮询
- 修掉停止抓取后轮询清不掉的僵尸请求

**🔧 重构（净效果为零，但要说明）**

- AI 后端在开发中途试过一版「内置模型 + 复用本机已装 Ollama」的双后端，发布前判定为纯负担予以删除——用户机器上多半没有 Ollama，留着只会多一套探测 / 预热 / 卸载分支，以及一个永远探测失败的状态字段。
  最终产品只保留内置模型这一条路，**`status()` 里不再有任何 ollama 字段**，并有测试脚本专门钉住这一点防止回退。
- `评分逻辑`从 `server.js` 剥离为独立模块 `score.js`，纯函数、无依赖，可单独测试。

---

### 📦 打包分发（仓库内工具，不随应用运行）

- 一键生成干净的分发目录（`工具/打包.js`），带安全断言：一旦发现私人数据目录就中止
- Inno Setup 安装包，装到用户目录、免管理员权限
- 内置瘦身：剔除上游依赖里只用于构建的源码树，分发体积从 661.7 MB 降到 412.7 MB
- 打包后可用 `工具/验证打包.js` 做真验收：起隔离实例 + 从该目录加载 native 引擎

---

### 🧪 测试

- 33 个验证脚本纳入版本控制（社区惯例：测试代码入库，只排除测试产物）
- 去掉脚本里的本机硬编码路径，改为相对 `__dirname`，换台机器也能跑
- 新增 `测试脚本/README.md` 说明运行方式与 13 个排查工具
